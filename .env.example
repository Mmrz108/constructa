# Postgres (local Docker or Vercel/Neon)
DATABASE_URL=postgresql://user:password@localhost:5434/constructa

# Better Auth
BETTER_AUTH_SECRET=replace-with-a-long-random-string
BETTER_AUTH_URL=http://localhost:3000

# Optional: force data-URL uploads locally (default on Vercel)
# UPLOAD_MODE=data
